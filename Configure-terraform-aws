Install Terraform on Windows, Configure
Download VS Cod, add Hasicorp Terraform autocomplete extension
Download and Install AWS CLI on Windows
Create IAM user in AWS, attching Admin access, Access key and secret access key
Configure IAM user - aws configure
Creating directory and Create EC2 Instance in AWS using Terraform
======================================================================
provider "aws" {
  region = "region"
}
resource "aws_instance" "Demo" {
  ami           = "instance_id"
  instance_type = "instance_type"
  key_name = "key_name"
  tags = {
    Name = "instance_name"
  }
}
